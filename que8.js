//que8

8.

function check(obj1,a,b) {
    if((obj1.name==a) && (obj1.id==b)){
      return "true";
    }else{
      return "false";
    }
    
    //return ((obj1.name===a) && (obj1.id===b))?'true':'false';
}

